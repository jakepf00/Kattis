#include<bits/stdc++.h>

using namespace std;

int main() {
    int numLights, length;
    cin >> numLights >> length;
    int road[length];
    for (int i = 0; i < length; i++) {
        road[i] = 0;
    }
    int lights[numLights][3];
    for (int i = 0; i < numLights; i++) {
        
    }
}
