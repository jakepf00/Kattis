#include<bits/stdc++.h>
using namespace std;

int main() {
    string a;
    double b;
    cin >> a >> b;
    if (size(a) <= (int) b) cout << size(a) << endl;
    else cout << fixed << setprecision(10) << b << endl;
}
