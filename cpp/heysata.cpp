#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    char a;
    string b;
    cin >> n >> a >> b;
    if (b.find(a) != string::npos) cout << "Unnar fann hana!" << endl;
    else cout << "Unnar fann hana ekki!" << endl;
}
